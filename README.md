# noterino-lambda
the evil backend for the `noterino` app

Features:
- [X] setup build pipeline
- [X] setup deploy pipeline
- [ ] build zip package
- [ ] deploy to aws
- [ ] `POST /document`
- [ ] `GET /document`
- [ ] `POST /document`
