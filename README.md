# noterino-lambda
the evil backend for the `noterino` app

Features:
- [ ] setup build pipeline
- [ ] build zip package
- [ ] deploy to aws
- [ ] `POST /document`
- [ ] `GET /document`
- [ ] `POST /document`
